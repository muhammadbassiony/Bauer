name: bauer-api
base: ubuntu@24.04
version: '0.1' # just for humans. Semantic versioning is recommended
summary: Automate copy update changes # 79 char long summary
description: |
    Bauer is a Go application that extracts document content, suggestions
    (proposed edits), and comments from Google Docs using the Google Docs
    API and Google Drive API.
platforms:
    amd64:

extensions:
    - go-framework

parts:
    go-framework/install-app:
        organize:
            bin/bauer-api: usr/local/bin/bauer-api

    runtime-tools:
        plugin: nil
        stage-packages:
            - git
            - ca-certificates

    gh-cli:
        plugin: dump
        source: https://github.com/cli/cli/releases/download/v2.63.2/gh_2.63.2_linux_amd64.tar.gz
        organize:
            gh_2.63.2_linux_amd64/bin/gh: usr/bin/gh
        stage:
            - usr/bin/gh
